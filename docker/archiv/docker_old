RUN conda create --name training_env environment.yml python=3.8 -y

# Make RUN commands use the new environment:
SHELL ["conda", "run", "-n", "training_env", "/bin/bash", "-c"]

RUN conda activate env

#conda install ipykernel -y
#python -m ipykernel install --user --name flow_ood --display-name "Python flow_ood"

RUN conda install pytorch torchvision torchaudio cudatoolkit=10.2 -c pytorch -c=conda-forge -y
RUN conda install -c conda-forge cmocean -y

RUN conda install -c intel scikit-learn -y
RUN conda install -c anaconda requests -y
RUN conda install -c conda-forge tensorboardx -y

RUN conda install h5py -y


######

#RUN useradd -d /lui lui
RUN apt-get install -y git
#RUN mkdir /lui
#RUN chown -R lui:lui /lui
#USER lui
#RUN cd /lui
#RUN git clone https://github.com/rubo77/gluon/ gluon
#RUN cd gluon


#RUN cd /lui
#RUN ls

#COPY ~/.ssh/id_rsa /home/lui/.ssh/id_rsa


############################################################################

# add ssh key
#RUN \
#    ssh-keygen -t rsa -P '' -f ~/.ssh/id_rsa && \
#    cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys && \
#    chmod 0600 ~/.ssh/authorized_keys

#ADD ssh_config /root/.ssh/config

